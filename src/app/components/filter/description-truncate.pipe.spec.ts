import { DescriptionTruncatePipe } from './description-truncate.pipe';

describe('DescriptionTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
